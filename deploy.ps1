$A="Updated assets";$B=git config user.name;if(-not $B){Start-Process "https://github.com/login";Write-Host "Auth required." -ForegroundColor Yellow;return};try{git pull origin main;git add .;git commit -m $A;git push origin main;Write-Host "Success" -ForegroundColor Green}catch{Write-Host "Error: $_" -ForegroundColor Red}
$A="Updated assets";$B=git config user.name;if(-not $B){Start-Process "https://github.com/login";Write-Host "Auth required." -ForegroundColor Yellow;return};try{git pull origin main;git add .;git commit -m $A;git push origin main;Write-Host "Success" -ForegroundColor Green}catch{Write-Host "Error: $_" -ForegroundColor Red}